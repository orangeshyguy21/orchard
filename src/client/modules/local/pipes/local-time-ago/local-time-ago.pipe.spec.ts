import {LocalTimeAgoPipe} from './local-time-ago.pipe';

describe('LocalTimeAgoPipe', () => {
	it('create an instance', () => {
		const pipe = new LocalTimeAgoPipe();
		expect(pipe).toBeTruthy();
	});
});
