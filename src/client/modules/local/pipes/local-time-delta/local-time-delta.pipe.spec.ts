/* Local Dependencies */
import {LocalTimeDeltaPipe} from './local-time-delta.pipe';

describe('LocalTimeDeltaPipe', () => {
	it('create an instance', () => {
		const pipe = new LocalTimeDeltaPipe();
		expect(pipe).toBeTruthy();
	});
});
