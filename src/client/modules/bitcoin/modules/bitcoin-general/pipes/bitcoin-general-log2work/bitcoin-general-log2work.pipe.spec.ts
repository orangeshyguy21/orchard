import {BitcoinGeneralLog2workPipe} from './bitcoin-general-log2work.pipe';

describe('BitcoinGeneralLog2workPipe', () => {
	it('create an instance', () => {
		const pipe = new BitcoinGeneralLog2workPipe();
		expect(pipe).toBeTruthy();
	});
});
