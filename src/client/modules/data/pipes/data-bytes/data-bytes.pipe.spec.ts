/* Local Dependencies */
import {DataBytesPipe} from './data-bytes.pipe';

describe('DataBytesPipe', () => {
	it('create an instance', () => {
		const pipe = new DataBytesPipe();
		expect(pipe).toBeTruthy();
	});
});
