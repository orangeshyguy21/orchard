export enum MintAnalyticsInterval {
	day = 'day',
	week = 'week',
	month = 'month',
	custom = 'custom',
}

export enum MintDatabaseType {
	sqlite = 'sqlite',
	postgres = 'postgres',
}

export enum MintActivityPeriod {
	day = 'day',
	three_day = 'three_day',
	week = 'week',
}
